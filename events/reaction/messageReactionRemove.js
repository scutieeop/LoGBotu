const { EmbedBuilder } = require('discord.js');module.exports = {  name: 'messageReactionRemove',  async execute(client, reaction, user) {    if (user.bot) return;    if (reaction.partial) {      try {        await reaction.fetch();      } catch (error) {        console.error('Tepki getirilirken hata oluştu:', error);        return;      }    }    const message = reaction.message;    if (message.partial) {      try {        await message.fetch();      } catch (error) {        console.error('Mesaj getirilirken hata oluştu:', error);        return;      }    }    if (!message.guild) return;    const handleReactionRole = require('./messageReactionAdd').handleReactionRole;    if (handleReactionRole) {      await handleReactionRole(client, reaction, user, false);    }  }}; 