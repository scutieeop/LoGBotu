const { EmbedBuilder } = require('discord.js');module.exports = {  name: 'logkanalları',  description: 'Tüm log kanallarını ve durumlarını listeler',  async execute(message, args, client) {    const logSetup = client.config.logSetup || {};    const enabledSettings = logSetup.enabled || {};    const channels = logSetup.channels || {};    const webhooks = logSetup.webhooks || {};    const statusEmojis = {      active: '🟢',      inactive: '🔴',      notConfigured: '⚪'    };    const logTypes = [      { id: 'mesaj', name: 'Mesaj Logları', channel: 'message-logs', description: 'Mesaj gönderme, silme, düzenleme işlemleri' },      { id: 'uye', name: 'Üye Logları', channel: 'member-logs', description: 'Sunucuya giriş, çıkış, yasaklama işlemleri' },      { id: 'ses', name: 'Ses Logları', channel: 'voice-logs', description: 'Ses kanalı giriş/çıkış ve durum değişiklikleri' },      { id: 'kanal', name: 'Kanal Logları', channel: 'channel-logs', description: 'Kanal oluşturma, silme, güncelleme işlemleri' },      { id: 'rol', name: 'Rol Logları', channel: 'role-logs', description: 'Rol oluşturma, silme, güncelleme işlemleri' },      { id: 'emoji', name: 'Emoji Logları', channel: 'emoji-logs', description: 'Emoji ekleme, silme, güncelleme işlemleri' },      { id: 'davet', name: 'Davet Logları', channel: 'invite-logs', description: 'Davet oluşturma, kullanım işlemleri' },      { id: 'server', name: 'Sunucu Logları', channel: 'server-logs', description: 'Sunucu ayarları ve genel değişiklikler' }    ];    const fields = logTypes.map(type => {      const isEnabled = enabledSettings[type.id] !== false;       const statusEmoji = isEnabled ? statusEmojis.active : statusEmojis.inactive;      const channelId = channels[type.channel];      const channel = channelId ? message.guild.channels.cache.get(channelId) : null;      const channelMention = channel ? `<#${channel.id}>` : 'Ayarlanmamış';      const hasWebhook = webhooks && webhooks[type.channel];      const webhookStatus = hasWebhook ? '✅' : '❌';      return {        name: `${statusEmoji} ${type.name}`,        value: `> **Kanal:** ${channelMention}\n> **Webhook:** ${webhookStatus}\n> **Açıklama:** ${type.description}`,        inline: false      };    });    const embed = new EmbedBuilder()      .setTitle('📋 Log Kanalları ve Ayarları')      .setDescription('Sunucunuzda yapılandırılan log kanalları ve durumları aşağıda listelenmiştir. Her log türü için webhook ayarlarını ve aktiflik durumlarını görebilirsiniz.')      .setColor('#5865F2')      .addFields(fields)      .setFooter({ text: `${message.guild.name} | Log Kanalları Durumu` })      .setTimestamp();    const defaultLogChannelId = client.config.logChannelId;    if (defaultLogChannelId) {      const defaultLogChannel = message.guild.channels.cache.get(defaultLogChannelId);      if (defaultLogChannel) {        embed.addFields({          name: '📝 Varsayılan Log Kanalı',          value: `> <#${defaultLogChannel.id}>\n> *Özel log kanalı ayarlanmamış log türleri için kullanılır*`,          inline: false        });      }    }    embed.addFields({      name: '💡 Log Kanallarını Düzenleme',      value: '> `.logayarla [log-türü] [açık/kapalı]` komutuyla log türlerini aktif/pasif yapabilirsiniz.\n> `.logsistemikur` komutuyla log sistemini baştan kurabilirsiniz.',      inline: false    });    await message.reply({ embeds: [embed] });  }}; 